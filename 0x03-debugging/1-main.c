#include "main.h"

/**
 *  * main - causes an infinite loop
 *   * Return: 0
 */

int main(void)
{
	int i;

	printf("lnfinite loop incoming :(\n");

	i = 0;
	/**
	 *while (i < 10)
	 *{
	 * putchar(i);
	 *}
	 */
	printf("lnfinite loop avoided! \\o/\n");

	return (0);
}
